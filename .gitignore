node_modules
dist
out
.DS_Store
*.log*
*.tsbuildinfo
ROADMAP.md

# Dev-only scripts and generated test fixtures — not for committing
scripts/dev/inject-fake-media.mjs
mocks/fat_*.tcq

# This project uses pnpm (see packageManager + preinstall guard in package.json).
# Block accidental npm/yarn lockfiles from sneaking into the repo.
package-lock.json
yarn.lock
