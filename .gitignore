node_modules
dist
dist-web
out

# Tauri
src-tauri/target
src-tauri/gen/schemas
# Icons are generated from build/icon.png at build time (scripts/gen-icons.mjs).
src-tauri/icons
.DS_Store
*.log*
*.tsbuildinfo
ROADMAP.md

# This project uses pnpm (see packageManager + preinstall guard in package.json).
# Block accidental npm/yarn lockfiles from sneaking into the repo.
package-lock.json
yarn.lock
